### PR 9：强化 YAML Schema 校验结果展示

功能描述：

```
本 PR 优化 YAML 校验结果的前端展示方式，将校验结果拆分为 YAML 语法、顶层字段、场景字段、人物一致性和来源章节绑定等检查项，让评委能够直观看到系统对结构化剧本输出的质量保障。
```

实现思路：

```
- 将后端校验结果映射为前端检查列表。
- 使用通过、警告、错误三种状态展示不同校验结果。
- 对缺失字段和 speaker 不一致问题展示具体路径。
- 在校验区域增加“重新校验”和“导出 YAML”操作入口。
```

测试方式：

```
- 正常 YAML 能显示全部通过。
- 手动删除 conflict 字段后能显示对应错误。
- 手动修改 speaker 后能显示人物一致性错误。
```